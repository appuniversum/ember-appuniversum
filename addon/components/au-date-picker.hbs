<div class="au-date-picker">
  {{#if @label}}
    <AuLabel for={{@id}}>{{@label}}</AuLabel>
  {{/if}}

  <duet-date-picker
    disabled={{@disabled}}
    buttonLabel={{@buttonLabel}}
    identifier={{@id}}
    value={{@value}}
    min={{@min}}
    max={{@max}}
    first-day-of-week={{@first-day}}
    {{on "duetChange" this.handleDuetDateChange}}
    {{prop localization=this.localization}}
    {{prop dateAdapter=this.adapter}}
    ...attributes
    ></duet-date-picker>
</div>